# Server Configuration
PORT=5000
NODE_ENV=production

# Database Configuration
DB_HOST=localhost
DB_USER=DB_USER
DB_PASSWORD=
DB_NAME=activity_calendar

# JWT Secret for Authentication
# Generate a strong random string (you can use: require('crypto').randomBytes(64).toString('hex'))
JWT_SECRET=your_jwt_secret_key_here

# React App 
# Only needed in development when running React and Express separately
REACT_APP_API_URL=http://localhost:5000/api